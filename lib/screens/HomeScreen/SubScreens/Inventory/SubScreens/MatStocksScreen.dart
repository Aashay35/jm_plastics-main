import 'package:flutter/material.dart';
class MatStocksScreen extends StatelessWidget {
  const MatStocksScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MatStocksScreen'),
      ),
      body: Container(),
    );
  }
}
