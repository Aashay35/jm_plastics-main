import 'package:flutter/material.dart';
class BeltsBearingsScreen extends StatelessWidget {
  const BeltsBearingsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('BeltsBearingsScreen'),
      ),
      body: Container(),
    );
  }
}
