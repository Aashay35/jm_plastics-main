import 'package:flutter/material.dart';
class PackagingScreen extends StatelessWidget {
  const PackagingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PackagingScreen'),
      ),
      body: Container(),
    );
  }
}
