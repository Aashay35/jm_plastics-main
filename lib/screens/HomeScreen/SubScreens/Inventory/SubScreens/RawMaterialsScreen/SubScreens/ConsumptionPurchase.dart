import 'package:flutter/material.dart';
class ConsumptionPurchase extends StatelessWidget {
  const ConsumptionPurchase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Text('Consumption Purchase')
        ],
      ),
    );
  }
}
